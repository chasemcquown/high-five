<div class="comments">
	{{#each this}}
	<section class="comment">
		<div class="meta">
			{{user.username}}
		</div>
		<div class="text">
			{{comment_text}}
		</div>
	</section>
	<p>IF YOU SEE THIS THEN COMMENTS HANDLEBARS IS WORKING</p>
	{{/each}}
</div>